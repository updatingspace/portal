---
sidebar_position: 1
slug: /intro
title: Введение в UpdSpace
description: Обзор платформы UpdSpace и проекта AEF
---

# UpdSpace Platform

**UpdSpace** — мультитенантная SaaS-платформа для игровых сообществ. Первый tenant — **AEF** (aef.updspace.com) — игровой интранет с голосованиями, событиями и интеграциями.

## 🎯 Ключевые особенности

### Архитектура
- **Микросервисы**: Python-first (Django + Ninja), но API-контракты позволяют переписать на Go/Rust
- **Multi-tenant**: Изоляция данных по `tenant_id`, tenant определяется по subdomain
- **BFF Pattern**: Фронтенд не хранит токены, сессия в HttpOnly cookie

### Безопасность
- **Нет self-signup**: Только application → approve администратором
- **SSO через UpdSpaceID**: Единственная система идентичности
- **OAuth providers**: Не создают аккаунт, только linking к существующему user

### Модули
- **Portal Core**: Сообщества, команды, посты, профили
- **Voting**: Голосования с номинациями и опциями
- **Events**: Календарь событий с RSVP
- **Activity**: Лента активности + коннекторы (Steam, Discord, Minecraft)

## 📊 Текущий статус

| Компонент | Статус | Описание |
|-----------|--------|----------|
| UpdSpaceID | ✅ Production | OIDC, magic-link, MFA, passkeys |
| BFF | ✅ Production | Session management, proxy |
| Access | ✅ Production | RBAC, permission checks |
| Portal | ✅ MVP | Communities, teams, posts |
| Voting | ✅ MVP | Polls, votes, results |
| Events | ✅ MVP | RSVP, attendance |
| Activity | 🔶 Early MVP | Feed, basic connectors |

## 🏃 Quick Start

```bash
# Запуск dev-окружения
./scripts/dev-up.sh

# Доступ
# Frontend: http://aef.localhost/
# ID Frontend: http://id.localhost/
# BFF API: http://aef.localhost/api/v1/*
```

## 📁 Структура проекта

```
aef-vote/
├── services/           # Backend микросервисы
│   ├── id/             # UpdSpaceID (Identity Provider)
│   ├── bff/            # Backend For Frontend
│   ├── access/         # Access Control (RBAC)
│   ├── portal/         # Portal Core
│   ├── voting/         # Голосования
│   ├── events/         # События
│   └── activity/       # Активность + интеграции
├── web/                # Frontend приложения
│   ├── portal-frontend/  # Основной портал (React + Vite)
│   └── id-frontend/      # IdP фронтенд
├── infra/              # Docker Compose, конфигурации
├── documentation/      # Эта документация (Docusaurus)
└── scripts/            # Dev scripts
```

## 🔗 Полезные ссылки

- [Архитектура](/docs/architecture/overview) — Обзор системы
- [Quick Start](/docs/guides/quick-start) — Запуск локально
- [API Reference](/docs/api/overview) — Документация API
- [Статус разработки](/docs/status/current) — Что работает сейчас

## 🤖 Для AI-агентов

Если вы AI-ассистент, работающий с этим проектом:

1. **Читайте AGENTS.md** в корне репозитория — инструкции для агентов
2. **Проверяйте статус**: Не все функции реализованы, см. [текущий статус](/docs/status/current)
3. **Учитывайте multi-tenancy**: Все данные изолированы по `tenant_id`
4. **BFF обязателен**: Фронтенд не хранит токены, все запросы через BFF
